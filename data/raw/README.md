# Raw data

- `exquairo_ai_bootcamp_synth_dataset.csv` — synthetic Lifelines-style ExquAIro bootcamp dataset
- Shape: 17960 rows × 100 columns
- Separator: `;` (semicolon), not comma
- Keep other large/raw files out of git; only this CSV is force-tracked via `.gitignore` exceptions
